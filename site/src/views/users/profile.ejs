<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/profile.css">
</head>
<body>
    <%- include('../partials/header') %>
    <section id="perfil">
        <div class="container">
            <h2>Bienvenido: <%= `${user.name} ${user.lastname}` %></h2>
            <div>
                <img class="profile-img" src="/images/users/<%= user.src %>" />
            </div>
            <ul>
                <li>Tu correo: <a href="mailto:<%= user.email %>"><%= user.email %></a></li>
            </ul>
        </div>	
    </section>
	<%- include('../partials/footer') %>    
</body>
</html>